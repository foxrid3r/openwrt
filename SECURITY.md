# Security

This repository is intended to contain only reusable firmware source and configuration templates.

Do not commit device backups, `/etc/shadow`, password hashes, SSH host private keys, real Wi-Fi credentials, VPN keys, certificates containing private keys, or site/customer-specific secrets.

Before publishing changes, run the repository secret scan described in `PUBLISHING_CHECKLIST.md`.
